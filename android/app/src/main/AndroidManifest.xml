<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Разрешения для погоды и геолокации -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Разрешение для MyTarget рекламы -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Разрешение для уведомлений (API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Разрешения для точных будильников (для надежной работы таймера) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:label="Водный баланс"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:enableOnBackInvokedCallback="true"
        tools:replace="android:label">
        <!-- Конфигурация RuStore Pay SDK согласно документации 9.1.0 -->
        <meta-data
            android:name="console_app_id_value"
            android:value="@string/CONSOLE_APPLICATION_ID" />
        <meta-data
            android:name="sdk_pay_scheme_value"
            android:value="@string/SDK_PAY_SCHEME" />
        <meta-data
            android:name="app_scheme_value"
            android:value="@string/SDK_PAY_SCHEME" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- RuStore deeplink для платежей -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="@string/SDK_PAY_SCHEME"/>
            </intent-filter>
        </activity>
        <!-- MyTarget активность для рекламы -->
        <activity 
            android:name="com.my.target.common.MyTargetActivity" 
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"/>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        
        <!-- Запросы для банковских приложений (RuStore Pay SDK) -->
        <!-- СБП - Система быстрых платежей -->
        <package android:name="ru.nspk.c2bmobile" />
        <package android:name="ru.nspk.c2bmobile.test" />
        
        <!-- SberPay -->
        <package android:name="ru.sberbankmobile" />
        <package android:name="ru.sberbankmobile.dev" />
        
        <!-- T-Pay (Tinkoff) -->
        <package android:name="com.idamobile.tinkoff.android" />
        <package android:name="com.tinkoff.payment.android" />
        
        <!-- Другие популярные банковские приложения -->
        <package android:name="ru.alfabank.mobile.android" />
        <package android:name="com.rbs.mobile.android" />
        <package android:name="ru.vtb24.mobilebanking.android" />
        <package android:name="ru.raiffeisen.client.android" />
        
        <!-- Intent для открытия банковских приложений через схемы -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="sbp"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="sberpay"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="tinkoff"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="alfabank"/>
        </intent>
    </queries>
</manifest>